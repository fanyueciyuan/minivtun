# 跨设备开发 - 保存和恢复进度指南

## 当前状态快照

**保存时间**: 2026-01-19
**当前设备**: Mac (Darwin 21.6.0)
**Git Commit**: 3c166dd + 255cd5f
**工作目录**: /Users/liyang/VPS/minivtun/minivtun

---

## 方法 1: 仅保存代码 (推荐 - 最简单)

### 📤 在当前设备 (Mac)

```bash
# 1. 确认所有代码已推送
cd /Users/liyang/VPS/minivtun/minivtun
git status

# 应该显示:
# On branch master
# Your branch is up to date with 'origin/master'.
# Untracked files: (文档文件 .md)

# 2. 查看远程仓库地址
git remote -v

# 3. 记录 Git 仓库 URL (如果需要)
```

### 📥 在新设备上

```bash
# 1. 克隆仓库
git clone <your-repo-url>
cd minivtun

# 2. 验证代码
git log --oneline -3
# 应该看到:
# 255cd5f - Fix HMAC authentication mechanism
# 3c166dd - Add HMAC authentication interface
# 8fa7424 - Initial commit

# 3. 编译测试
cd src
make clean && make

# 完成! 代码已同步
```

**优点**: 最简单,推荐用于生产部署
**缺点**: 文档文件(.md)不包含在内

---

## 方法 2: 保存代码 + 文档 (完整)

### 📤 在当前设备 (Mac)

```bash
cd /Users/liyang/VPS/minivtun/minivtun

# 1. 创建 docs 分支保存所有文档
git checkout -b docs

# 2. 添加所有文档文件
git add *.md
git add *.patch
git add *.sh

# 3. 查看将要提交的文件
git status

# 4. 提交文档
git commit -m "Add documentation files

- HMAC fix complete documentation
- Security audit reports
- Implementation guides
- Quick start guides
- Project summary

Date: 2026-01-19"

# 5. 推送 docs 分支
git push origin docs

# 6. 切回 master 分支
git checkout master
```

### 📥 在新设备上

```bash
# 1. 克隆仓库 (包含所有分支)
git clone <your-repo-url>
cd minivtun

# 2. 获取代码 (master 分支)
git checkout master
cd src && make clean && make && cd ..

# 3. 获取文档 (docs 分支)
git checkout docs

# 或者同时查看两个分支:
git checkout master
git checkout docs -- '*.md' '*.patch' '*.sh'  # 拉取文档到 master

# 完成! 代码和文档都已同步
```

**优点**: 完整保存,包含所有文档
**缺点**: 需要额外操作

---

## 方法 3: 使用压缩包备份 (离线备份)

### 📤 在当前设备 (Mac)

```bash
cd /Users/liyang/VPS/minivtun

# 1. 创建完整备份
tar -czf minivtun-backup-20260119.tar.gz minivtun/

# 2. 仅备份文档
cd minivtun
tar -czf minivtun-docs-20260119.tar.gz *.md *.patch *.sh

# 3. 传输文件 (选择一种方式)
# - 方式 A: U盘拷贝
# - 方式 B: 网盘 (iCloud/百度网盘/Google Drive)
# - 方式 C: scp 传输到新设备
scp minivtun-backup-20260119.tar.gz user@new-computer:~/
```

### 📥 在新设备上

```bash
# 1. 解压备份
tar -xzf minivtun-backup-20260119.tar.gz
cd minivtun

# 2. 配置 Git (如果是新设备)
git config user.name "Yang"
git config user.email "liyangyijie@gmail.com"

# 3. 连接到远程仓库
git remote add origin <your-repo-url>
git fetch origin

# 4. 编译测试
cd src && make clean && make

# 完成!
```

**优点**: 不依赖网络,完整备份
**缺点**: 手动传输

---

## 方法 4: 文档推送到独立仓库

### 📤 在当前设备 (Mac)

```bash
cd /Users/liyang/VPS/minivtun/minivtun

# 1. 创建文档仓库 (在 GitHub 上)
# 例如: minivtun-docs

# 2. 初始化文档仓库
mkdir ../minivtun-docs
cp *.md *.patch *.sh ../minivtun-docs/
cd ../minivtun-docs

git init
git add .
git commit -m "Initial documentation commit"
git remote add origin <docs-repo-url>
git push -u origin master
```

### 📥 在新设备上

```bash
# 1. 克隆代码仓库
git clone <code-repo-url>
cd minivtun

# 2. 克隆文档仓库
cd ..
git clone <docs-repo-url>

# 现在有两个目录:
# minivtun/      - 代码
# minivtun-docs/ - 文档

# 完成!
```

**优点**: 代码和文档分离,清晰
**缺点**: 需要管理两个仓库

---

## 推荐方案对比

| 方案 | 难度 | 完整性 | 推荐场景 |
|------|------|--------|---------|
| 方法 1: 仅代码 | ⭐ 最简单 | 代码完整 | 生产部署 |
| 方法 2: 代码+文档(分支) | ⭐⭐ 简单 | 100% | **推荐** |
| 方法 3: 压缩包 | ⭐⭐ 简单 | 100% | 离线备份 |
| 方法 4: 双仓库 | ⭐⭐⭐ 中等 | 100% | 长期维护 |

---

## 快速操作 (推荐方法 2)

### 📤 当前设备 - 保存文档

```bash
cd /Users/liyang/VPS/minivtun/minivtun
git checkout -b docs
git add *.md *.patch *.sh
git commit -m "Add documentation (2026-01-19)"
git push origin docs
git checkout master
```

### 📥 新设备 - 恢复所有内容

```bash
# 克隆仓库
git clone <your-repo-url>
cd minivtun

# 拉取文档到 master 分支
git checkout docs -- '*.md' '*.patch' '*.sh'

# 编译
cd src && make clean && make

# 完成!
```

---

## 验证清单

在新设备上验证所有内容已正确同步:

### ✅ 代码文件 (master 分支)

```bash
ls -la src/crypto_wrapper.h    # 应该存在
ls -la src/crypto_openssl.c    # 应该存在
ls -la src/crypto_mbedtls.c    # 应该存在
ls -la src/client.c             # 应该存在
ls -la src/server.c             # 应该存在
```

### ✅ 文档文件 (如果使用方法 2)

```bash
ls -la *.md                     # 应该看到 17 个 .md 文件
ls -la *.patch                  # hmac_auth_fix.patch
ls -la *.sh                     # 脚本文件
```

### ✅ Git 历史

```bash
git log --oneline -5
# 应该看到:
# 255cd5f - Fix HMAC authentication mechanism
# 3c166dd - Add HMAC authentication interface
# 8fa7424 - Initial commit
```

### ✅ 编译测试

```bash
cd src
make clean
make
./minivtun -h  # 应该显示帮助信息
```

---

## 常见问题

### Q1: 如果 Git 远程仓库地址忘记了?

```bash
# 在当前设备查看
cd /Users/liyang/VPS/minivtun/minivtun
git remote -v

# 或者查看 .git/config
cat .git/config | grep url
```

### Q2: 新设备没有 SSH 密钥?

```bash
# 生成新的 SSH 密钥
ssh-keygen -t ed25519 -C "liyangyijie@gmail.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到 GitHub: Settings → SSH and GPG keys → New SSH key
```

### Q3: 如果要在新设备上继续开发?

```bash
# 1. 克隆仓库
git clone <your-repo-url>
cd minivtun

# 2. 创建新分支进行开发
git checkout -b feature/new-feature

# 3. 修改代码...

# 4. 提交
git add .
git commit -m "Add new feature"
git push origin feature/new-feature

# 5. 在 GitHub 上创建 Pull Request
```

### Q4: 两台设备同时开发如何同步?

```bash
# 在设备 A 修改后
git add .
git commit -m "Update from device A"
git push origin master

# 在设备 B 上拉取
git pull origin master

# 解决冲突 (如果有)
git status  # 查看冲突文件
# 手动解决冲突后
git add .
git commit -m "Resolve conflicts"
git push origin master
```

---

## 文件列表清单

### 代码文件 (已在 GitHub master 分支)

- [x] src/crypto_wrapper.h
- [x] src/crypto_openssl.c
- [x] src/crypto_mbedtls.c
- [x] src/client.c
- [x] src/server.c

### 文档文件 (本地,可选推送到 docs 分支)

- [ ] 快速开始.md
- [ ] PROJECT_SUMMARY.md
- [ ] HMAC_FIX_README.md
- [ ] 文档索引.md
- [ ] 认证机制修复方案.md
- [ ] 安全审计报告.md
- [ ] 初学者指南.md
- [ ] GIT_PUSH_COMMANDS.md
- [ ] HMAC修复应用指南.md
- [ ] 跨设备开发指南.md (本文件)
- [ ] hmac_auth_fix.patch
- [ ] apply_hmac_fix.sh
- [ ] push_to_github.sh

---

## 下一步行动

### 立即执行 (保存进度)

```bash
# 在当前 Mac 上执行:
cd /Users/liyang/VPS/minivtun/minivtun
git checkout -b docs
git add *.md *.patch *.sh
git commit -m "Add complete documentation (2026-01-19)"
git push origin docs
git checkout master

echo "✅ 进度已保存到 GitHub docs 分支"
```

### 在新设备上执行 (恢复进度)

```bash
# 在新电脑上执行:
git clone <your-repo-url>
cd minivtun
git checkout docs -- '*.md' '*.patch' '*.sh'
cd src && make clean && make

echo "✅ 进度已恢复,可以继续开发"
```

---

**当前状态**: 代码已在 GitHub master 分支,文档在本地
**推荐操作**: 使用方法 2 (推送 docs 分支)
**预计时间**: 5 分钟

🚀 **准备好后执行上面的"立即执行"命令即可!**
